# Copy to .env only when a component actually needs configuration.
# Never commit the copied .env file.

POSTGRES_DB=observatorio_socrates
POSTGRES_USER=observatorio_app
POSTGRES_PASSWORD=CHANGE_ME_WITH_A_LONG_UNIQUE_SECRET

# Local-only endpoints. Do not expose either service publicly.
N8N_HOST=127.0.0.1
OLLAMA_BASE_URL=http://host.docker.internal:11434
